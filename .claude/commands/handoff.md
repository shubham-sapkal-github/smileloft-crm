---
description: Write up this session so the work survives being picked up later
argument-hint: optional title
---

Write a handoff document to `docs/handoffs/NNN-<slug>.md` covering this session.
$ARGUMENTS

Cover:

- What was asked
- What was done, with file paths
- What was deliberately **not** done, and why
- What is still open
- How to verify it
- Anything the next person would otherwise have to rediscover the hard way

Facts only. Where you're unsure whether something works, say so rather than
implying it was checked.
