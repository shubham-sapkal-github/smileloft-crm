---
description: Write a plan before any code gets written
argument-hint: what needs building
---

Write a spec for: **$ARGUMENTS**

1. Read the relevant existing code first. Change nothing during this pass.
2. Restate the requirement in your own words and show me *just that* before
   filling in the rest. If I've been misunderstood, I want to catch it in a
   sentence.
3. Once I confirm, fill in `docs/specs/TEMPLATE.md`, saved as the next free
   number in `docs/specs/`.
4. Name what is deliberately out of scope, and why — that section is not
   optional filler, it's the point.
5. Put every open question in one batch at the end.

Do not write implementation code. The output of this command is one markdown
file.
